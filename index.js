const chalk = require('chalk');

console.log(chalk.red("I don't think this is what you wanted to do..."));
console.log(chalk.yellow('If you want to install the eztwitch-cli, you can do so by running:'));
console.log(chalk.green('npm i -g eztwitch'));
console.log(chalk.yellow('After your installation you can run eztwitch in the console.'));
